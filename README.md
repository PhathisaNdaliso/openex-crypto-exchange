# openex-crypto-exchange
A lightweight simulated crypto exchange that supports BTC/USD trading (limit &amp; market orders), user wallets (simulated balances), an order book, basic matching engine, simulated market data feed, REST API &amp; WebSocket streaming, and a React-based dashboard. Everything is open-source components and libraries.
